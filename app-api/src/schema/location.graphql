type Location {
    countryCode: String!
    country: String!
    state: String
    city: String
    county: String
    postCode: String!
}

extend type Query {
    getGeoCodeLocation(latitude: String!, longitude: String!): Location
    getLocationByPostCode(postCode: String!): Location
}